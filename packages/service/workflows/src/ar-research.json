{
  "id": "ar-research",
  "name": "AR Research",
  "active": false,
  "versionId": "00000000-0000-4000-8000-00000000ae01",
  "settings": {
    "executionOrder": "v1"
  },
  "nodes": [
    {
      "id": "execute-workflow-trigger",
      "name": "Execute Workflow Trigger",
      "type": "n8n-nodes-base.executeWorkflowTrigger",
      "typeVersion": 1,
      "position": [
        0,
        0
      ],
      "parameters": {}
    },
    {
      "id": "load-domain-context",
      "name": "Load Domain Context",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "position": [
        220,
        0
      ],
      "credentials": {
        "postgres": {
          "id": "ar-postgres",
          "name": "AR Postgres"
        }
      },
      "parameters": {
        "operation": "executeQuery",
        "query": "WITH handover AS (\n  SELECT nullif($1::jsonb, 'null'::jsonb) AS item\n),\nclaim AS (\n  SELECT\n    CASE\n      WHEN h.item ->> 'run_id' ~ '^[0-9]{1,18}$'\n      THEN (h.item ->> 'run_id')::bigint\n    END AS run_id\n  FROM handover h\n),\nrun AS (\n  SELECT r.domain_id\n  FROM runs r\n  WHERE r.id = (SELECT run_id FROM claim)\n)\n-- The first node after the trigger, and the shape the parent design fixes for every\n-- workflow in this set (.specs/2026-08-19-research-pipeline-port.md, section 4): the\n-- domain's personas and taxonomy, read as ONE query answering exactly one row. Both\n-- halves are argued in full on ar-ingest's Load Domain Context, which asks the same\n-- question of the same tables and is not repeated here. What is written out below is\n-- the half that differs, which is how this canvas gets a domain at all.\n\n-- It gets one off a RUN, and off no member of the hand-over item. The caller opened\n-- nothing: the runs row this reads is the one ar-dispatch opened for the claimed\n-- topic and ar-ingest is accounting for, and its id is what travels. A domain named\n-- on the item as well would be a second statement of something that row already\n-- carries, and two statements of one fact can disagree with nothing reporting it.\n-- What a research pass records is charged to a run, so the domain it is filed under\n-- is the run's.\n\n-- The whole hand-over item binds as ONE jsonb parameter rather than one parameter per\n-- member. An item reaching this trigger by hand may be anything at all,\n-- JSON.stringify makes every one of those a valid jsonb literal, and the members are\n-- then read where a comment can say what each one is instead of inside an expression\n-- no gate reads. The nullif is the other half of that: JSON.stringify(null) binds the\n-- JSON null literal rather than SQL NULL, so an invocation carrying no item at all\n-- would otherwise arrive here as a value.\n\n-- ->> answers NULL for every jsonb that is not an object, so an item that is not one\n-- needs no guard in front of the read: the pattern test is handed NULL, the CASE\n-- answers NULL, and nothing raises over a shape somebody else chose. It renders a\n-- number as text as readily as a string, which is what lets one read cover both\n-- spellings an id can arrive in.\n\n-- The digit bound is {1,18} rather than +, and the difference between them is a\n-- raise. A longer run of digits matches + and then overflows the bigint cast, which\n-- is an error this node could not survive: eighteen digits sits inside bigint's\n-- nineteen, and no runs id is going to reach it.\n\n-- Three readings come out of this row and they are not two. A null run_id means the\n-- item named no run this statement could read. A run_id with a null domain_id means\n-- it named one that matches no runs row. A domain row means the pass resolved, and\n-- an empty list beside it is a domain that states no personas, no categories, no\n-- terms or no criteria. Read them in that order: a length says nothing until both\n-- have been read.\n\n-- Nothing here refuses. Every intention this pass is asked to drain is a row\n-- somebody else already raised and an operator already ruled on, so a refusal at the\n-- top of this workflow leaves those rows unstamped with nothing recording why. What\n-- this answers instead is a row saying what it could and could not resolve.\n\n-- run_id travels as the parameter rather than as a column read back, so it survives\n-- a run id that resolves to nothing. It has to travel because this node's answer\n-- REPLACES the item the trigger was handed: the rest of the hand-over is the claimed\n-- topic, its id and the two interval bounds a proposed gap is clamped against, and\n-- whatever node below needs those reaches all of it at\n-- $('Execute Workflow Trigger').first().json, which is the handle every node here\n-- has on the invocation. Reading that is the proposing group's rather than this\n-- node's.\n\n-- Every list is ordered, which nothing downstream requires and every reader benefits\n-- from: two passes over one unchanged domain hand the nodes below the same lists in\n-- the same order, so a diff between two runs is about the candidates rather than\n-- about the order a planner happened to return rows in.\nSELECT\n  (SELECT run_id FROM claim) AS run_id,\n  (SELECT domain_id FROM run) AS domain_id,\n  -- The domain row, cut to the members a research pass reads, and shorter than the\n  -- one ar-ingest and ar-score fetch. Those two carry feature_version and\n  -- embedding_model because a feature vector is composed on those canvases, and\n  -- nothing here composes one: a projection copied whole would be two columns\n  -- travelling to no reader, and a reader added later would be reading a pin nobody\n  -- here maintains. What is left is the identity an operator reads a pass by and the\n  -- settings a display vocabulary comes out of. NULL rather than an empty object\n  -- when the item named no run this statement could resolve, which is the second of\n  -- the three readings above.\n  (SELECT jsonb_build_object(\n     'id', d.id,\n     'slug', d.slug,\n     'name', d.name,\n     'settings', d.settings)\n     FROM domains d\n    WHERE d.id = (SELECT domain_id FROM run)) AS domain,\n\n  -- The personas. system_text is operator-owned prose and is the trusted half of\n  -- what prompt-frame.ts composes around an untrusted chunk, and the whole list\n  -- travels rather than the one row a step needs: which role a step is framed\n  -- against is that step's to decide, and picking a row out of a list already here\n  -- is a read rather than a second query per role. A persona is a row and is never\n  -- written into a file, and no node below may put a candidate's own text where one\n  -- belongs.\n  COALESCE(\n    (SELECT jsonb_agg(jsonb_build_object(\n       'id', p.id,\n       'role', p.role,\n       'system_text', p.system_text)\n       ORDER BY p.role, p.id)\n       FROM personas p\n      WHERE p.domain_id = (SELECT domain_id FROM run)),\n    '[]'::jsonb) AS personas,\n\n  -- The categories, which are the buckets the terms below are filed under. key\n  -- rather than id is what a reader downstream matches on: a library reading a\n  -- category reads its key, so the surrogate id travels for an operator's benefit\n  -- and the key is what the code matches.\n  COALESCE(\n    (SELECT jsonb_agg(jsonb_build_object(\n       'id', c.id,\n       'key', c.key,\n       'name', c.name,\n       'parent_id', c.parent_id)\n       ORDER BY c.key, c.id)\n       FROM categories c\n      WHERE c.domain_id = (SELECT domain_id FROM run)),\n    '[]'::jsonb) AS categories,\n\n  -- The terms, joined to their category's key rather than carrying category_id.\n  -- Four members and this exact spelling, because the libraries reading this one\n  -- list each read a different subset of it: feature-version.ts takes all four,\n  -- features.ts takes pattern and category, and static-gate.ts takes pattern, weight\n  -- and polarity. A term list selected without the join satisfies one reader and\n  -- starves the others, with nothing failing.\n  COALESCE(\n    (SELECT jsonb_agg(jsonb_build_object(\n       'category', c.key,\n       'pattern', t.pattern,\n       'weight', t.weight,\n       'polarity', t.polarity)\n       ORDER BY c.key, t.pattern, t.id)\n       FROM terms t\n       JOIN categories c ON c.id = t.category_id\n      WHERE c.domain_id = (SELECT domain_id FROM run)),\n    '[]'::jsonb) AS terms,\n\n  -- The criteria, joined to their category the same way. Nothing in a research pass\n  -- scores anything: criteria are ar-score's subject, and a pass here is about what\n  -- is known of one candidate rather than about how a finding measures up against a\n  -- position the domain stated. They are selected because the design fixes the\n  -- taxonomy as what this node fetches, and the taxonomy is these three tables, so a\n  -- node group added below reads them without a second lookup and without a second\n  -- branch to reason about.\n  COALESCE(\n    (SELECT jsonb_agg(jsonb_build_object(\n       'id', k.id,\n       'category', c.key,\n       'value', k.value,\n       'kind', k.kind)\n       ORDER BY c.key, k.kind, k.id)\n       FROM criteria k\n       JOIN categories c ON c.id = k.category_id\n      WHERE k.domain_id = (SELECT domain_id FROM run)),\n    '[]'::jsonb) AS criteria",
        "options": {
          "queryReplacement": "={{ JSON.stringify($json ?? null) }}"
        }
      }
    },
    {
      "id": "drain-approved-intentions",
      "name": "Drain Approved Intentions",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "position": [
        440,
        0
      ],
      "credentials": {
        "postgres": {
          "id": "ar-postgres",
          "name": "AR Postgres"
        }
      },
      "parameters": {
        "operation": "executeQuery",
        "query": "-- Where an approved intention becomes work, and the first node on this canvas that\n-- decides what the pass is about. The node above resolved a domain; this one reads\n-- the rulings a person has already made against it, and hands the groups below one\n-- item per candidate to research.\n\n-- NOTHING HERE APPROVES ANYTHING. research_pool in src/db/schema/entities.ts is the\n-- gate, and what fills it happens elsewhere entirely: an intention is raised by\n-- ar-ingest or by ar-score, and approved_at is written by a person through\n-- scripts/approve.ts and, after that, through the API and the UI. This statement\n-- reads the result of those rulings and makes none of its own. No node on this canvas\n-- writes approved_at, and no branch here stands in for one.\n\n-- A PENDING ROW IS INVISIBLE TO THIS QUERY, which is what the gate buys at the drain.\n-- It is not selected, not counted, and not named by the group that closes this pass,\n-- so a domain whose queue nobody has worked through drains nothing and spends\n-- nothing. That is the outcome rather than a gap: an intention nobody consented to is\n-- exactly what this pipeline must not act on, and never seeing one is the cheapest\n-- way of not acting on it.\n\n-- STATUS AND THE TIMESTAMPS ARE TWO ACCOUNTS OF ONE ROW, and this statement reads\n-- only the first. research_pool.status records where a row stands and nothing refuses\n-- a writer that sets any member at any time, while research_pool_approval_check at\n-- the foot of that table reads approved_at and researched_at alone. So a row stamped\n-- approved whose approved_at is still NULL is storable, and it is selected here.\n\n-- What refuses it is the database, at the write, and that is the design rather than\n-- an oversight. The record group below inserts the entity_research row and stamps\n-- researched_at in ONE statement, so a subject nobody approved takes neither half. A\n-- branch here would not be the same thing: a branch governs the writes one workflow\n-- issues and leaves every other writer to its own habits, which is the argument that\n-- table makes in full.\n\n-- What that costs is worth naming rather than implying. A row in that state reaches\n-- the model call before the write refuses it, so the call is billed and nothing is\n-- recorded, and the row stays open and comes round again. It is a misconfiguration\n-- somebody has to fix, and a run reporting a refused write is how they find out.\n\n-- OLDEST FIRST, so a queue worked top-down empties rather than churns. The row at\n-- risk of never being drained is the one that has waited longest, and a newest-first\n-- pass pushes it further down every time an intention is raised. listPending in\n-- scripts/approve.ts orders the review queue the same way and for the same reason, so\n-- the operator reading the queue and the pass draining it agree about which end\n-- matters.\n\n-- created_at does not order these rows on its own. now() is the transaction's start\n-- time, so every intention one pass raised carries the same value to the microsecond\n-- and timestamptz stores nothing finer. id breaks the tie, which is what makes two\n-- passes over one unchanged queue take the same batch rather than merely look stable.\n\n-- A BOUNDED BATCH, and the bound is a spending ceiling rather than paging.\n-- research_pool says what it is for: a queue nobody is watching must not turn into a\n-- single expensive run, so a hundred approved rows are researched over many passes.\n-- Twenty five is the number the rest of this set bounds a batch with, both\n-- AR_DISPATCH_BATCH_CAP in scripts/workflow-markers.ts and ar-ingest's\n-- MAX_MODEL_CALLS_PER_RUN holding it, and it is written out here because a statement\n-- can splice nothing and no marker declares it. Moving it to ENV_DEFAULTS is that\n-- edit plus its own entry in that table rather than something to imply here.\n\n-- IT IS NOT THE CALL CEILING. This bounds the rows the pass CARRIES: the projection\n-- below reads documents for every one of them, and the gate above the model group\n-- runs every one of them through a validator. What the pass may SPEND is declared\n-- again in that group, over what survives the gate, which is never more rows than\n-- this and is usually fewer. The two bound different things and coincide in value,\n-- and neither is derived from the other.\n\n-- A ROW PAST THE BOUND IS NOT DROPPED. It keeps its approval and its open stamp, so\n-- the next pass finds it at the head of this same order. That is unlike the cap in\n-- ar-dispatch, which drops rows already claimed and rescheduled and leaves them a\n-- whole interval, and unlike the ceiling in ar-ingest, where a document left outside\n-- the bound is never offered again at all.\n\n-- A ROW THAT CANNOT BE RESEARCHED IS THE COST OF OLDEST FIRST, and settling it\n-- belongs to the groups below. Nothing stamped stays at the head, so an intention the\n-- record group refuses is fetched first again by the next pass and paid for again.\n-- research_pool.researched_at is where that is argued and where the answer is stated:\n-- a row the drain cannot act on is closed with its reason kept rather than left open,\n-- which is the failure the design this port draws from met and fixed.\n\n-- ONE ITEM PER CANDIDATE. A Postgres node maps its statement over the items it was\n-- handed and passes on what the statement returned, and Load Domain Context above\n-- answers exactly one row, so this node runs once and answers as many items as it\n-- drained. Every group below is one item's work: the gate validates one name, the\n-- framing node prepares one chunk, and the record group writes one result.\n\n-- Which also means this node's answer REPLACES the context item, the way that item\n-- replaced the one the trigger was handed. The personas, the categories and the terms\n-- are not gone: a node below reaches them at $('Load Domain Context').first().json,\n-- and the claimed topic a proposed gap is clamped against is still at\n-- $('Execute Workflow Trigger').first().json.\n\n-- AN EMPTY DRAIN EMITS NO ITEM, and a node handed no item does not run, so a pass\n-- with nothing approved ends here rather than below. That is the ordinary state of\n-- most passes, intentions being raised on every ingest and ruled on when somebody has\n-- time. What a pass that drained nothing records is the canvas topology's to answer\n-- rather than this statement's.\n\n-- TWO PARAMETERS, in declaration order: the run and the domain. Both are read off the\n-- context item above rather than out of the hand over a second time. That node has\n-- already resolved the domain off the run, and deriving it a second way here is how\n-- the two start being able to disagree. A NULL domain selects nothing, which is where\n-- a pass started by hand ends.\n\nSELECT\n-- WHAT TRAVELS IS CUT TO WHAT THE GROUPS BELOW READ, on the rule Load Domain Context\n-- above applies to its own projection: a column travelling to no reader is a column a\n-- reader added later starts maintaining without knowing it. So created_at and\n-- approved_at order this batch and do not travel, and finding_id is read by the\n-- documents subquery below and travels no further than that.\n  $1::bigint AS run_id,\n\n  -- The intention itself, and the only handle on it that survives the item\n  -- replacement above. The record group addresses this row by id when it stamps\n  -- researched_at, and nothing below re-derives which intention it is working on: a\n  -- second derivation of that is how a result comes to close a row it did not come\n  -- from. domain_id travels beside it so an item says which domain it belongs to\n  -- without reaching back for the context, and the predicate below is what makes it\n  -- the parameter's.\n  p.id AS pool_id,\n  p.domain_id,\n\n  -- The exact strings an operator approved, carried rather than composed.\n  -- research_pool.search_terms is the gate's substance: the approval was given to\n  -- these and not to a description of what would be assembled later, so a node below\n  -- that built its own would be issuing something nobody ruled on. An empty list is a\n  -- real state and means an intention nobody could turn into a query, which is a row\n  -- the groups below close rather than search for nothing.\n  p.search_terms,\n\n  -- The subject, resolved one hop through alias_of, and NOT the column the pool row\n  -- carries. entities.alias_of states the reader's rule, COALESCE(alias_of, id), and\n  -- why a merge is a pointer rather than a rewrite: a placeholder keeps its own row\n  -- and its own history and readers resolve through it. So an intention raised\n  -- against a placeholder is researched as the subject it turned out to be, and the\n  -- entity_research row is filed under that subject rather than under the row that\n  -- stood in for it. This is the id that column takes.\n  --\n  -- One hop and no further, which is that column's rule as well: a chain resolves to\n  -- whatever its first hop names and a self-alias resolves to the row itself, so\n  -- neither loops a reader here any more than anywhere else.\n  --\n  -- NULL where the intention names no subject at all, which research_pool.entity_id\n  -- calls an ordinary state rather than a gap: an intention can be raised from a\n  -- finding whose subject nothing has attributed, and the terms above are what make\n  -- it researchable anyway. What a pass may ask about a candidate carrying no name is\n  -- the gate below's to decide rather than this statement's.\n  e.id AS entity_id,\n  e.name AS entity_name,\n\n  -- The documents behind the candidate, and the whole of the material this pass is\n  -- asked to research it against. Every one of them is a row this domain already\n  -- captured: nothing here reaches outside the corpus, and what a model is later\n  -- handed is a chunk prepared from these bodies rather than anything fetched.\n  --\n  -- WHICH DOCUMENTS. The finding that raised the intention is always one of them,\n  -- because it is the occasion an operator approved and a bound able to cut it would\n  -- offer a model everything except the reason the row exists. The rest are the\n  -- documents of the other findings attributed to the same subject, which is what\n  -- makes this a candidate's corpus rather than one document's.\n  --\n  -- The subject test reads both spellings, the id the intention named and the id it\n  -- resolved to, so a placeholder's own findings and its target's are both here. What\n  -- it does not reach is a THIRD row aliased to the same subject: that would be a\n  -- resolution per finding rather than one join, and its findings are simply not\n  -- offered. Saying so is the point rather than fixing it here.\n  --\n  -- BOUNDED AT FIVE PER CANDIDATE, which is what keeps this projection from carrying\n  -- a subject's whole history. Bodies travel whole, this node answering one item per\n  -- drained row, so what a pass carries is five documents times the batch bound\n  -- below. The occasioning document is ordered first so the bound can never drop it,\n  -- and newest first after that, on the reading ar-ingest's own corpus window takes:\n  -- recent material is what a reader wants, and the id is what makes recent\n  -- deterministic.\n  --\n  -- The set and the order are two different jobs, which is why there are two ORDER\n  -- BYs. The inner one decides WHICH five and reads the aggregate, the outer one\n  -- emits them ascending by id, so two passes over one unchanged corpus hand the\n  -- groups below the same list in the same order.\n  --\n  -- GROUP BY d.id alone, the primary key functionally determining the other three\n  -- members, which is what lets one document carrying several of the subject's\n  -- findings arrive once rather than once per finding.\n  --\n  -- Four members each. id is what a citation names, and researchBriefErrors in\n  -- src/lib/research-brief.ts judges an answer's citations against exactly the ids\n  -- offered here, so this list IS the offered set. url is provenance for a person\n  -- reading a result. body is what a chunk is built from. parse_status travels beside\n  -- it because a document whose parse failed still has a row and may still have an\n  -- empty body, and the group preparing a chunk has to be able to say which of the\n  -- two it is looking at.\n  --\n  -- An empty list is a real answer rather than a failure: an intention naming no\n  -- subject and no finding has nothing behind it, and it is the terms above that were\n  -- approved in that case.\n  COALESCE(\n    (SELECT jsonb_agg(jsonb_build_object(\n       'id', b.id,\n       'url', b.url,\n       'parse_status', b.parse_status,\n       'body', b.body)\n       ORDER BY b.id)\n       FROM (\n         SELECT d.id, d.url, d.parse_status, d.body\n           FROM findings f\n           JOIN documents d ON d.id = f.document_id\n          WHERE f.domain_id = p.domain_id\n            AND (f.id = p.finding_id\n                 OR (p.entity_id IS NOT NULL\n                     AND f.entity_id IN (p.entity_id, e.id)))\n          GROUP BY d.id\n          ORDER BY bool_or(f.id = p.finding_id) DESC NULLS LAST, d.id DESC\n          LIMIT 5) b),\n    '[]'::jsonb) AS documents\n\n-- The pool row, and the two joins that name its subject. The first reads the entity\n-- the intention named and the second reads the one that names resolves to, both LEFT\n-- because research_pool.entity_id is nullable and an intention naming no subject is\n-- an ordinary row rather than one to drop. Neither join can fail to find its target\n-- where there is one: alias_of carries ON DELETE no action, so an entity an alias\n-- points at cannot be deleted out from under it.\nFROM research_pool p\nLEFT JOIN entities n ON n.id = p.entity_id\nLEFT JOIN entities e ON e.id = COALESCE(n.alias_of, n.id)\n\n-- The predicate, three rules and none standing in for another. domain_id keeps the\n-- pass inside the domain its run was opened for, and researching another domain's\n-- approved intention would be work nobody in this pass consented to. status is the\n-- ruling. researched_at IS NULL is the row still being open, which is the second half\n-- of what that column reserves: closed rather than researched, so a row the drain\n-- closed without a search is out of this set too and does not come round again.\nWHERE p.domain_id = $2::bigint\n  AND p.status = 'approved'\n  AND p.researched_at IS NULL\nORDER BY p.created_at, p.id\nLIMIT 25",
        "options": {
          "queryReplacement": "={{ $json.run_id }}, {{ $json.domain_id }}"
        }
      }
    },
    {
      "id": "gate-candidate-names",
      "name": "Gate Candidate Names",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        660,
        0
      ],
      "parameters": {
        "jsCode": "__INLINE:validate-entity-name.ts__\n// The capability gate, and the last thing standing between a name\n// somebody else's text produced and a line of a prompt. The library\n// above is spliced in whole and holds the whole decision: the four\n// checks, the order they run in, the cap, the two patterns and the\n// roster of non-answers. Nothing below states a rule of its own\n// about what a name may be.\n//\n// WHAT THIS BOUNDS IS WHAT THE RESEARCH STEP MAY BE ASKED, NOT WHAT\n// IT MAY ANSWER, and the two halves sit at opposite ends of this\n// canvas on purpose. A name reaching here was written by a model\n// that read a document and said what it decided the document was\n// about, and it travels into the prompt the group below frames. So\n// the question asked here is whether a name could change the SHAPE\n// of what is asked, a scheme or an address or a line break or a\n// template opener, and not whether it reads like a hostile\n// instruction.\n//\n// An instruction-shaped name PASSES, which the library's own header\n// argues at length and which is worth reading before widening\n// anything here. By the time a name is validated it is only ever a\n// subject line: nothing it can say makes a request reach somebody\n// else's host, because a name never reaches a host at all. A filter\n// on how instruction-like a name reads would drop real subjects and\n// would still leave the next phrasing nobody thought of.\n//\n// What the ANSWER may be is judged in the record group, by two\n// libraries this node does not carry. researchBriefErrors in\n// src/lib/research-brief.ts refuses an answer whose shape is wrong\n// or whose citations name documents the pass never offered, and\n// sanitize-md.ts reduces its text before any of it is stored. A\n// name refused here reaches neither, because it never reaches the\n// call whose answer they judge.\n//\n// WHAT THE OPERATOR APPROVED WAS NOT THIS STRING, which is the\n// asymmetry the gate exists for. listPending in scripts/approve.ts\n// reads research_pool rows and joins no entity, so the ruling was\n// given to the stored search terms and to a subject id. The name is\n// joined in at the drain, off entities.name, and that column was\n// written by an extraction rather than read by a person. The terms\n// passed a person; the name did not.\n//\n// NO ROSTER IS PASSED, so ENTITY_NON_ANSWERS applies. A per domain\n// roster would be a member of domains.settings, and DomainSettings\n// in src/db/schema/domains.ts declares none: adding one is a schema\n// edit and an operator's decision rather than a read of a key\n// nobody declared. The default is extraction-shaped rather than\n// domain-shaped, being what a model answers when the document named\n// nothing, so it is the right default for every domain until one\n// says otherwise.\n//\n// A CANDIDATE CARRYING NO NAME IS REFUSED, and empty is the reason.\n// The drain says in as many words that this is the gate's to\n// decide, and the answer comes from the far end of the pass:\n// entity_research.entity_id is NOT NULL, so a candidate naming no\n// subject has nowhere for a result to land and the record group\n// could not store what a call came back with. Refusing it here is\n// refusing it above the ceiling, the framing node and the model,\n// which is the difference between a row that costs nothing and a\n// row that costs a call and stores nothing.\n//\n// ONLY AN ACCEPTED NAME TRAVELS, and it is the NORMALIZED one. The\n// trim and the whitespace collapse are part of what was validated,\n// so what goes on is the string the allowlist was tested against\n// rather than the one the drain read.\n//\n// A REFUSAL CARRIES NO NAME AT ALL, which is the library's own\n// answer shape and its reason: a reader that forgot to check reads\n// nothing rather than the text it was about to interpolate. Two\n// members go with it, search_terms and documents, and they go for a\n// stronger version of the same reason. Those are the only members a\n// prompt is built from, so a refused candidate leaves this node\n// structurally unable to reach one whatever a node below does or\n// forgets to do. What it keeps is what names the row an outcome is\n// about.\n//\n// A REJECTION IS AN OUTCOME AND IS CARRIED FORWARD, not dropped.\n// Every item leaves here carrying name_ok, and a refused one\n// carries name_reason beside it: one of ENTITY_NAME_REJECTIONS, a\n// stable token rather than a sentence, because these are counted\n// and written into a ledger. The token carries nothing off the\n// value it refused, so a name the gate would not accept does not\n// reach a log through this member either.\n//\n// Dropping the refusals instead would make a pass that refused\n// every candidate indistinguishable from a pass that drained\n// nothing, and those are opposite reports: one is a queue nobody\n// can act on, the other is a queue waiting on a person.\n//\n// name_ok rather than gate_ok, because the gate on this canvas\n// already means research_pool and the two must not read as one\n// thing. That one is an approval and is the database's; this one is\n// a capability bound and is a library's.\n//\n// WHY A REFUSED CANDIDATE LEAVES ITS research_pool ROW UNSTAMPED.\n// Nothing between the drain and the record group writes a row, this\n// node included, and the record group's stamp is welded to its\n// insert: ONE statement writes the entity_research row and stamps\n// researched_at. A refused candidate produces no research, so that\n// statement never runs for it, and the row keeps its approval and\n// its open stamp.\n//\n// So the next pass fetches it FIRST, the drain ordering oldest\n// first, and refuses it again. That repetition is free: the refusal\n// happens above the ceiling, the framing node and the model, so\n// nothing is billed and nothing is stored. What it spends is a row\n// at the head of the queue, and that is the point rather than the\n// cost. The queue is where an operator looks, and a row that came\n// back is a report that something upstream wants fixing.\n//\n// The closable kind of refusal is a different one, and the\n// difference is worth holding on to. research_pool.researched_at\n// reserves its stamp for a row the drain closes with its reason\n// kept, an intention with nothing issuable in search_terms or a\n// subject researched recently enough, because leaving one of those\n// open means paying for the same search on every later pass. A name\n// this gate refuses costs nothing to meet again, and closing it\n// would retire an intention a person approved on a judgement about\n// a string nobody ruled on. What clears it is a repair upstream:\n// the subject renamed, or merged through alias_of, or the row\n// closed by hand.\n//\n// CARDINALITY. One item in, one item out, and no node is read by\n// name: everything decided here arrived on the item.\n\n// What a refused candidate keeps, and the whole of it. A list\n// rather than a delete from a copy of the row, so a member the\n// drain adds later has to be named here before it can travel on a\n// refusal, which is the direction that fails safe.\nconst REFUSAL_KEEPS = ['run_id', 'pool_id', 'domain_id', 'entity_id'];\n\nreturn $input.all().map((item) => {\n  const row = item.json;\n  const verdict = validateEntityName(row.entity_name);\n\n  if (verdict.ok) {\n    return {\n      json: {\n        ...row,\n        entity_name: verdict.name,\n        name_ok: true,\n        name_reason: null,\n      },\n    };\n  }\n\n  const kept = Object.fromEntries(\n    REFUSAL_KEEPS.map((member) => [member, row[member] ?? null]),\n  );\n\n  return {\n    json: { ...kept, name_ok: false, name_reason: verdict.reason },\n  };\n});"
      }
    },
    {
      "id": "select-model-connector",
      "name": "Select Model Connector",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "position": [
        880,
        0
      ],
      "executeOnce": true,
      "credentials": {
        "postgres": {
          "id": "ar-postgres",
          "name": "AR Postgres"
        }
      },
      "parameters": {
        "operation": "executeQuery",
        "query": "WITH\n-- Where the address of the model this pass calls is read, and the only node in this\n-- group that can read one. A Code node opens no connection and a spliced library\n-- imports nothing, so the endpoint and the model name arrive here as data or the\n-- nodes below have nothing to call. ar-ingest carries a node of this name asking the\n-- same question of the same table and argues the whole of it, including why the\n-- oldest llm row wins and why neither member is validated anywhere before this\n-- projection. What is written out here is the half that differs.\n\n-- ONE READ FOR THE PASS, on that node's reasoning. executeOnce runs this against the\n-- first item it was handed and answers once, and which service this deployment calls\n-- is a fact about the deployment rather than about any one candidate.\n\n-- THE FIRST ITEM IS A GATED CANDIDATE, AND MAY BE A REFUSED ONE. That is what parts\n-- this node from its sibling. Gate Candidate Names above answers one item per drained\n-- candidate whether or not the name was accepted, and a refusal keeps run_id among\n-- the four members it carries, so the parameter below resolves off either kind.\n-- Which of the two arrived first is a property of the drain's order and is not\n-- something this node has to know.\n\n-- TWO QUIET PASSES REACH DIFFERENT PLACES, and telling them apart is worth a line. A\n-- pass that drained nothing never reaches this node at all, executeOnce having no\n-- first item to run against. A pass whose whole batch the gate refused does reach it,\n-- reads a connector and spends nothing, the node below staging no candidate. Neither\n-- makes a call and only the second reads the table.\n\n-- One parameter, the run, read off the item this node was handed. It travels for the\n-- reason it travels everywhere on this canvas: the ledger row below is charged to the\n-- run and not to a candidate.\nconnector AS (\n  -- The llm connector, by kind, oldest first and capped at one. connectors is not\n  -- domain scoped and nothing on a domain row names one, so there is nothing here to\n  -- narrow by. The sibling states what the cap decides and what reconfiguring rather\n  -- than adding a rival buys.\n  SELECT c.id, c.name, c.config\n  FROM connectors c\n  WHERE c.kind = 'llm'\n  ORDER BY c.id\n  LIMIT 1\n)\n\nSELECT\n-- Two members read out of the config object, each read as text only where it IS text\n-- and with the empty string taken for an absence. connectors.config carries no type\n-- annotation, its shape being the client's business, so nothing has validated either\n-- of these and this projection is where they are first read.\n\n-- A null endpoint means there is nowhere to reach, and the node below refuses on it\n-- by name. That is the last place this pass can stop before a call is attempted\n-- against an address nobody configured, and stopping there rather than here is\n-- deliberate: a research pass that cannot call is still a pass whose gate refusals\n-- are worth reporting, and this node answers a row rather than a verdict.\n\n-- A null model is an ordinary state and not a refusal. A gateway that routes, or a\n-- runtime serving whatever it was started with, takes no model name, and\n-- llm_calls.model is nullable for exactly that.\n\n-- connector_id and connector_name are carried for a reader rather than for a caller.\n-- Nothing below binds either one. They travel because the node that made a call and\n-- the row that named the service should be readable together on the canvas.\n  $1::bigint AS run_id,\n  (SELECT c.id FROM connector c) AS connector_id,\n  (SELECT c.name FROM connector c) AS connector_name,\n  (SELECT nullif(CASE WHEN jsonb_typeof(c.config -> 'endpoint') = 'string'\n                      THEN c.config ->> 'endpoint'\n                 END, '')\n     FROM connector c) AS endpoint,\n  (SELECT nullif(CASE WHEN jsonb_typeof(c.config -> 'model') = 'string'\n                      THEN c.config ->> 'model'\n                 END, '')\n     FROM connector c) AS model",
        "options": {
          "queryReplacement": "={{ $json.run_id }}"
        }
      }
    },
    {
      "id": "apply-call-ceiling",
      "name": "Apply Call Ceiling",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1100,
        0
      ],
      "parameters": {
        "jsCode": "// Where a research pass decides how much it is going to spend, and\n// the first node on this canvas that decides anything about money.\n// Nothing above it opens a connection to a model and nothing below\n// it can reduce what this one lets through. The ceiling is applied\n// here, before a chunk is prepared, because preparing one is work\n// and the cheapest call is the one that was never staged.\n//\n// THE CEILING IS A DECLARED CONSTANT. MAX_RESEARCH_CALLS_PER_RUN\n// below is an integer written into this source, which is what\n// docs/architecture/01-invariants.md means by an explicit per run\n// ceiling: a bound a reviewer finds by reading the declarations\n// rather than by following the arithmetic. It is deliberately not a\n// settings marker, on the reasoning ar-ingest gives at the node of\n// this name. ENV_DEFAULTS in scripts/workflow-markers.ts is where a\n// value an operator tunes per deployment lives, and moving this one\n// there is that edit plus its own entry in that table rather than\n// something to imply here.\n//\n// IT IS THE SECOND BOUND THIS PASS MEETS AND NOT THE FIRST. Drain\n// Approved Intentions bounds the rows the pass CARRIES at twenty\n// five and says in as many words that what the pass may SPEND is\n// declared again here, over what survives the gate. The two bound\n// different things, they coincide in value, and neither is derived\n// from the other: this one can never stage more candidates than the\n// drain fetched and usually stages fewer.\n//\n// WHAT SURVIVES THE GATE is read off one field. Gate Candidate\n// Names wrote name_ok onto every drained candidate, true where\n// validateEntityName accepted the name and false with a token\n// beside it where it did not, and only an accepted one still\n// carries the members a prompt is built from. There is no second\n// reading to make here: that node already refused a candidate\n// carrying no name at all, and it dropped search_terms and\n// documents from every refusal so that nothing below it could reach\n// a prompt out of one whatever it forgot to check.\n//\n// A REFUSED CANDIDATE DOES NOT TRAVEL AND IS NOT LOST. It stops\n// here, having nothing to spend on, and stays reachable at\n// $('Gate Candidate Names').all() for the group that reports what\n// the pass did. Dropping it from the stream is not dropping it from\n// the account, and the two would be the same thing only if this\n// node were the last reader of that one.\n//\n// THE BUDGET GOES TO THE HEAD OF THE QUEUE, and the order is the\n// drain's rather than a policy of this node's. That statement\n// orders oldest first so a queue worked top down empties rather\n// than churns, and the row at risk of never being drained is the\n// one that has waited longest. Re-sorting here on anything else\n// would put that row at the bottom of a second ordering nobody\n// asked for. So the arrival order is the spending order, and two\n// passes over one unchanged queue spend on the same candidates.\n//\n// That is where this parts from ar-ingest, which sorts its own\n// staging by gate score: a document is worth reading in proportion\n// to what a gate made of it, and nothing ruled on it first. Here a\n// person ruled on every row in the batch, so there is no ranking\n// left to apply and waiting time is the only thing that separates\n// them.\n//\n// WHAT THE CEILING DROPS COMES BACK. A candidate left outside the\n// bound is stamped by nothing, keeps its approval and its open\n// researched_at, and is fetched first again by the next pass, the\n// drain ordering oldest first. That is unlike ar-ingest, where a\n// document left outside its ceiling is never offered again at all,\n// and it is what makes this bound a pacing decision rather than a\n// permanent one.\n//\n// THE CONNECTOR IS REFUSED RATHER THAN PASSED ON. A deployment with\n// no llm connector, or one whose config states no endpoint, has\n// nowhere to send a call, and this node stops the pass by name\n// instead of emitting nothing. Emitting nothing is\n// indistinguishable from a pass whose queue was empty, and a\n// deployment in that state would go on researching nothing quietly\n// for as long as nobody looked. What the refusal costs is the run\n// row the group below would have written. Nothing above this node\n// is undone, because nothing above this node wrote anything: the\n// drain reads and the gate decides, and the first write on this\n// canvas is in the record group.\n//\n// CARDINALITY. This node is handed one item, the connector, and\n// answers one per candidate it stages. The gate's items are read\n// back by name, for the reason everything on this canvas is: the\n// Postgres node in front of this one replaced the item that held\n// them.\n\n// Twenty five candidates, the same number the drain bounds its own\n// batch with and chosen against the same asymmetry: a pass is\n// started cheaply and then charged once per candidate for as long\n// as nobody looks at the bill. Written out rather than shared,\n// because a SQL statement can splice nothing and no marker declares\n// this number, so the two spellings are one bound maintained in two\n// places on purpose.\nconst MAX_RESEARCH_CALLS_PER_RUN = 25;\n\nconst connector = $input.first();\n\nif (connector === undefined) {\n  throw new Error(\n    '[ar-research] Apply Call Ceiling was handed no connector item, '\n    + 'which Select Model Connector answers whatever the table holds',\n  );\n}\n\nconst endpoint = connector.json.endpoint;\n\nif (typeof endpoint !== 'string' || endpoint === '') {\n  throw new Error(\n    '[ar-research] Apply Call Ceiling has no endpoint to spend '\n    + 'against: connectors holds no llm row whose config states one',\n  );\n}\n\n// Staged in the order the drain fetched them, which is the order\n// this walk reads them in, so the slice below takes the front of\n// the queue and nothing here re-orders anything.\nconst staged = [];\n\nfor (const item of $('Gate Candidate Names').all()) {\n  const row = item.json;\n\n  if (row.name_ok !== true) {\n    continue;\n  }\n\n  staged.push(row);\n}\n\n// Sliced rather than filtered, so the bound is the one declared\n// above and never a property of what happened to arrive. What is\n// carried forward is the four ids a row below is written against,\n// the name the gate normalized, and the two members the node below\n// builds a prompt out of. name_ok and name_reason stop here, having\n// done everything they were read for.\nreturn staged.slice(0, MAX_RESEARCH_CALLS_PER_RUN).map((row) => ({\n  json: {\n    run_id: row.run_id,\n    pool_id: row.pool_id,\n    domain_id: row.domain_id,\n    entity_id: row.entity_id,\n    entity_name: row.entity_name,\n    search_terms: row.search_terms,\n    documents: row.documents,\n  },\n}));"
      }
    },
    {
      "id": "prepare-model-prompt",
      "name": "Prepare Model Prompt",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1320,
        0
      ],
      "parameters": {
        "jsCode": "__INLINE:chunk.ts__\n__INLINE:prompt-frame.ts__\n// Where an approved intention becomes the one payload a model may\n// receive. Both libraries above are spliced in whole and are wired\n// together here, which is the only place they can meet:\n// src/lib/chunk.ts reduces the material behind a candidate to a\n// bounded chunk and refuses the ones that are not worth a call, and\n// src/lib/prompt-frame.ts takes that chunk and fences it so that\n// everything inside the fence is read as data and never as\n// instructions. Nothing below states a reduction rule or a framing\n// rule of its own.\n//\n// AND NOTHING ELSE GOES FORWARD, which is the whole reason this\n// node exists as a separate one. The item it answers carries the\n// two framed halves, the four ids a row below is written against,\n// the ids of the documents the pass offered, and the measurements\n// of the prompt. Neither the candidate's name nor its terms nor a\n// document body survives it. So no expression below can reach for\n// the stored text, and the model node is fed a prepared chunk\n// because there is nothing else on the item to feed it.\n//\n// THE TRUSTED HALF AND THE UNTRUSTED HALF ARRIVE AS TWO MEMBERS,\n// which is the framing library's structural claim rather than a\n// convenience. A caller wiring a model node has to decide which\n// channel each half goes in, and two members make that decision\n// visible on this canvas: the node below puts system in the system\n// message and data in the prompt, and neither can be confused for\n// the other by an expression that reached for one string.\n//\n// THE PERSONA IS A ROW AND NEVER A LITERAL. It arrives from the\n// domain's personas, fetched by Load Domain Context at the top of\n// this pass, and personas in src/db/schema/domains.ts argues at\n// length why no prompt string is written into a workflow file at\n// all: one file serves every domain, the text is what an operator\n// tunes most, and a prompt fixed on a canvas is lost at the next\n// import. The role read is the researcher, which is the role that\n// reads documents, and it is the same role ar-ingest frames\n// against. The two ask different things of it and neither writes\n// the ask down here.\n//\n// THERE IS A HEADER, AND THAT IS WHERE THIS PARTS FROM ar-ingest.\n// buildChunk takes a roster of fields and the values already\n// resolved for them, and writes a header line per field so a model\n// does not have to guess what is known. At ingest nothing has\n// resolved a field yet, so that node passes no roster and says so.\n// Here three things are known before the call and every one of them\n// is a fact about the ask rather than a guess about the answer: the\n// subject a person approved research on, the search terms they\n// approved with it, and the ids of the documents this pass is\n// handing over. Writing them into the header is the difference\n// between asking about a candidate and asking about a body of text\n// that happens to mention one.\n//\n// THE OFFERED IDS ARE IN THE HEADER BECAUSE AN ANSWER IS JUDGED\n// AGAINST THEM. researchBriefErrors in src/lib/research-brief.ts\n// refuses an answer whose citations name a document the pass never\n// offered, and a model that was never told the ids can only guess\n// at them. So the header states the set the answer will be held to.\n//\n// WHAT A CITATION CAN AND CANNOT BE WRONG ABOUT is worth saying\n// rather than implying. Each document is introduced in the body by\n// a marker this node writes, and the text after that marker is\n// somebody else's, so a document is free to spell a marker of its\n// own. What that buys is a mis-attribution INSIDE the offered set,\n// which the record group cannot tell from a genuine one. What it\n// cannot buy is a citation outside that set: the offered ids\n// travel on the item, the answer is judged against them, and a\n// forged id naming a document this pass never handed over is\n// refused with the row left unstamped.\n//\n// A DOCUMENT WITH NO TEXT IS NOT OFFERED. The drain carries\n// parse_status beside every body precisely so this node can tell\n// an empty body from a failed parse, and both come to the same\n// thing for the composition: a document whose body reduced to\n// nothing was never handed over, so its id is not among the\n// offered ones and an answer citing it is refused. A parse that\n// FAILED and still left text is offered, with the marker saying\n// so, because partial text is still text somebody captured and\n// hiding that from a reader would be worse than labelling it.\n//\n// A CANDIDATE WITH NOTHING BEHIND IT IS SKIPPED RATHER THAN\n// ASKED ABOUT. The drain says an empty documents list is a real\n// state, and buildChunk refuses a body that reduced to nothing.\n// This node does not set allowFieldsOnly, and the omission is the\n// decision: nothing on this canvas reaches outside the corpus, so\n// a call made with a subject line and no material would be a call\n// answered out of the model's own parameters, and the result would\n// be stored as this domain's research with nothing behind it. The\n// three identifying flags a fields-only chunk would be judged by\n// are left off the roster below for the same reason: they govern\n// that flag alone and this node never sets it.\n//\n// THE EXCERPT CEILING IS THE LIBRARY'S AND IT IS SHARED. All the\n// bodies of one candidate are composed into a single excerpt, and\n// that excerpt meets MAX_EXCERPT_CHARS exactly as one document's\n// does at ingest. So five documents behind a candidate share the\n// bound that one document has to itself, which is a real cost and\n// the right one: what is capped is what a call is charged for, and\n// the cap belongs to the prompt rather than to the number of rows\n// it was assembled from.\n//\n// TWO REFUSALS, and they are answered differently because they are\n// faults of different things. A chunk refusal is about one\n// candidate: nothing survived the boilerplate removal, or what\n// survived is too short to be worth reading. That candidate is\n// skipped and the pass carries on. A persona refusal is about the\n// operator's own text, is identical for every candidate in the\n// pass, and would otherwise be a run that quietly made no calls at\n// all, so it stops the pass by name. Which of the three the framing\n// library refused is what parts them, and CHUNK_EMPTY_REASON is the\n// one that belongs to the candidate.\n//\n// WHAT A SKIPPED CANDIDATE LOSES, and it is less than a skipped\n// document loses at ingest. Nothing between the drain and the\n// record group writes a row, so a candidate that does not reach a\n// call is stamped by nothing: it keeps its approval and its open\n// researched_at and is fetched first again by the next pass. A\n// candidate skipped because the corpus behind it holds no prose is\n// a candidate that will be skipped again until something upstream\n// captures material for it, and that repetition costs a place at\n// the head of the queue and nothing else.\n//\n// CARDINALITY. One item in, at most one out, and fewer whenever a\n// chunk was refused. Nothing below pairs against the node above\n// this one: every item carries its own pool_id, and the ledger\n// below pairs against THIS node by position.\n\n// The role whose system text this workflow speaks with.\n// personas.role is free text and carries no CHECK, the roles a\n// pipeline plays growing with the pipeline, so this is a lookup\n// into rows an operator wrote rather than a member of a closed set.\n// The seed writes three: this one reads documents, and ar-ingest\n// reads the same row to ask a different question of it.\nconst PERSONA_ROLE = 'researcher';\n\n// The header roster, in the order buildChunk writes it. No entry\n// carries an identifying flag, and the omission is deliberate:\n// that flag governs allowFieldsOnly alone, which this node never\n// sets, so writing one would be configuration nothing reads.\nconst RESEARCH_FIELDS = [\n  {\n    key: 'subject',\n    label: 'Subject',\n    fallback: '(the intention names no subject)',\n  },\n  {\n    key: 'terms',\n    label: 'Approved search terms',\n    fallback: '(the intention names no terms)',\n  },\n  {\n    key: 'documents',\n    label: 'Documents offered, by id',\n    fallback: '(no documents offered)',\n  },\n];\n\n// What separates the members of a list inside one header line.\n// buildChunk collapses every value to a single line, so this is\n// about what a reader and a model can both parse rather than about\n// layout.\nconst LIST_SEPARATOR = ', ';\n\n// What separates one document from the next in the composed body.\nconst DOCUMENT_SEPARATOR = '\\n\\n';\n\n// A document id in the one spelling an answer will be judged\n// against. canonicalId in src/lib/research-brief.ts is the\n// authority and this is a second copy of its rule, the splice\n// forbidding the import that would share one, so a change there is\n// an edit here. Both halves are the rule. A number is read only\n// when it is a positive safe integer, past which the value has\n// already lost digits and names no document. A string is read only\n// in the canonical decimal spelling, so 012 is refused rather than\n// read as twelve and one document cannot arrive under two ids.\nfunction canonicalDocumentId(value) {\n  if (typeof value === 'number') {\n    return Number.isSafeInteger(value) && value > 0\n      ? String(value)\n      : null;\n  }\n\n  if (typeof value !== 'string') {\n    return null;\n  }\n\n  return /^[1-9][0-9]*$/.test(value)\n    ? value\n    : null;\n}\n\n// The documents this pass is handing over, in the order the drain\n// answered them, cut to the ones there is text to hand over. A row\n// whose body reduced to nothing was never offered, so its id stays\n// out of the header and an answer citing it is refused downstream.\n// parse_status decides how a document is LABELLED and never\n// whether it is offered: text a failed parse left behind is still\n// text somebody captured.\nfunction offeredDocuments(value) {\n  const offered = [];\n\n  if (!Array.isArray(value)) {\n    return offered;\n  }\n\n  for (const entry of value) {\n    if (entry === null || typeof entry !== 'object') {\n      continue;\n    }\n\n    const id = canonicalDocumentId(entry.id);\n    const body = typeof entry.body === 'string'\n      ? entry.body.trim()\n      : '';\n\n    if (id === null || body === '') {\n      continue;\n    }\n\n    offered.push({ id, body, parsed: entry.parse_status === 'ok' });\n  }\n\n  return offered;\n}\n\n// How one document is introduced in the composed body, and the\n// whole of what a model is told about where a passage came from.\n// The marker is trusted text this node writes; everything after it\n// is somebody else's.\nfunction documentBlock(entry) {\n  const marker = entry.parsed\n    ? '[document ' + entry.id + ']'\n    : '[document ' + entry.id + ', partial parse]';\n\n  return marker + '\\n' + entry.body;\n}\n\n// The approved terms as one line. research_pool.search_terms is a\n// jsonb list and nothing has validated its members, so a member\n// that is not text is dropped rather than coerced into a header\n// line, and an empty list leaves the roster's stand-in standing.\nfunction termsText(value) {\n  if (!Array.isArray(value)) {\n    return '';\n  }\n\n  const terms = [];\n\n  for (const term of value) {\n    if (typeof term === 'string' && term.trim() !== '') {\n      terms.push(term.trim());\n    }\n  }\n\n  return terms.join(LIST_SEPARATOR);\n}\n\n// The system text of the persona holding a role, or the empty\n// string where there is none. Every member is typed on the way\n// out, because these entries came back through jsonb and nothing\n// has validated them: a row whose system_text is not text is a row\n// an operator has not finished, and it refuses below with the same\n// sentence a missing row does.\nfunction personaText(entries, role) {\n  for (const entry of entries) {\n    if (entry !== null && typeof entry === 'object' && entry.role === role\n      && typeof entry.system_text === 'string') {\n      return entry.system_text;\n    }\n  }\n\n  return '';\n}\n\nconst context = $('Load Domain Context').first().json;\nconst personas = Array.isArray(context.personas)\n  ? context.personas\n  : [];\nconst persona = personaText(personas, PERSONA_ROLE);\n\n// A pass with no researcher persona, or one whose text is not\n// text. Both are the same fault from here and both are pass wide,\n// so this refuses before a single chunk is built rather than once\n// per candidate.\nif (persona === '') {\n  throw new Error(\n    '[ar-research] Prepare Model Prompt has no system text for the '\n    + PERSONA_ROLE + ' persona: the domain states none, or the row '\n    + 'it states holds no text',\n  );\n}\n\nconst items = [];\n\nfor (const item of $input.all()) {\n  const row = item.json;\n  const offered = offeredDocuments(row.documents);\n  const ids = offered.map((entry) => entry.id);\n  const built = buildChunk({\n    fields: RESEARCH_FIELDS,\n    values: {\n      subject: row.entity_name,\n      terms: termsText(row.search_terms),\n      documents: ids.join(LIST_SEPARATOR),\n    },\n    body: offered.map(documentBlock).join(DOCUMENT_SEPARATOR),\n  });\n\n  if (!built.usable) {\n    continue;\n  }\n\n  const framed = promptFrame(persona, built.chunk);\n\n  if (!framed.usable) {\n    // A framing refusal that is not about the chunk is about the\n    // persona, and a persona fault repeats on every candidate in\n    // the pass. Refusing by name here is what parts an operator\n    // text fault from a queue with nothing readable behind it.\n    if (framed.reason !== CHUNK_EMPTY_REASON) {\n      throw new Error(\n        '[ar-research] Prepare Model Prompt cannot frame the '\n        + PERSONA_ROLE + ' persona: ' + framed.reason,\n      );\n    }\n\n    continue;\n  }\n\n  const promptChars = framed.system.length + framed.data.length;\n\n  // What the model node is fed, and what the two nodes after it\n  // read. The name is gone, the terms are gone and no body is\n  // here: the fenced form is the only spelling of any of them that\n  // leaves this node.\n  //\n  // offered_ids is the exception and is not one of them. It is a\n  // list of ids this node derived, not a member the drain handed\n  // over, and it travels because the record group judges the\n  // answer's citations against exactly this set. Deriving it a\n  // second time down there would be a second statement of what was\n  // offered, and two statements of that can disagree.\n  items.push({\n    json: {\n      run_id: row.run_id,\n      pool_id: row.pool_id,\n      domain_id: row.domain_id,\n      entity_id: row.entity_id,\n      offered_ids: ids,\n      system: framed.system,\n      data: framed.data,\n      prompt_chars: promptChars,\n      est_tokens: estimateTokens(promptChars),\n      fence_cuts: framed.fenceCuts,\n      forms_defanged: framed.formsDefanged,\n    },\n  });\n}\n\nreturn items;"
      }
    },
    {
      "id": "research-candidate",
      "name": "Research Candidate",
      "type": "@n8n/n8n-nodes-langchain.chainLlm",
      "typeVersion": 1.4,
      "position": [
        1540,
        0
      ],
      "retryOnFail": false,
      "onError": "continueRegularOutput",
      "parameters": {
        "promptType": "define",
        "text": "={{ $json.data }}",
        "messages": {
          "messageValues": [
            {
              "type": "SystemMessagePromptTemplate",
              "message": "={{ $json.system }}"
            }
          ]
        }
      }
    },
    {
      "id": "model-endpoint",
      "name": "Model Endpoint",
      "type": "@n8n/n8n-nodes-langchain.lmChatOpenAi",
      "typeVersion": 1,
      "position": [
        1540,
        220
      ],
      "retryOnFail": false,
      "credentials": {
        "openAiApi": {
          "id": "ar-model",
          "name": "AR Model"
        }
      },
      "parameters": {
        "model": "={{ $('Select Model Connector').first().json.model }}",
        "options": {
          "baseURL": "={{ $('Select Model Connector').first().json.endpoint }}",
          "timeout": 60000,
          "maxRetries": 0
        }
      }
    },
    {
      "id": "ledger-model-call",
      "name": "Ledger Model Call",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "position": [
        1760,
        0
      ],
      "credentials": {
        "postgres": {
          "id": "ar-postgres",
          "name": "AR Postgres"
        }
      },
      "parameters": {
        "operation": "executeQuery",
        "query": "WITH\n-- Where a call is accounted for. One row per call, charged to the run that made it,\n-- and src/db/schema/runs.ts argues at length what that buys: the runs row accounts\n-- for a pass as a whole, and these rows account for the same work one call at a time,\n-- which is the granularity the question of what a call cost is asked at. ar-ingest\n-- carries a node of this name writing the same table, and the attribution argument,\n-- the no-conflict-target argument and the position-pairing argument are all made\n-- there rather than repeated here.\n\n-- THE CEILING AND THIS ROW ARE ONE PROPERTY read from two ends. Apply Call Ceiling\n-- states what a pass WILL spend and these rows are what it did, so a ceiling that\n-- quietly stopped being applied reads exactly like a pass whose queue happened to be\n-- short. Neither half is enforced by anything in this schema, which is why the\n-- property is asserted over the built artifact instead.\n\n-- ONE ROW OUT PER CALL. The insert sits in a CTE and the outer SELECT names no FROM,\n-- so this node answers exactly one item for every item it was handed, and the group\n-- below reads a candidate that was called about whether or not the call came back\n-- with anything. An INSERT ... RETURNING on its own would emit nothing for a\n-- statement that wrote no row, and a research pass whose call failed still has a pool\n-- row to account for.\n\n-- THREE PARAMETERS, in declaration order: the run, the model name, and the prepared\n-- item this call was made from. The first two are read off Select Model Connector,\n-- which answered one row for the whole pass, so neither is a per call reading and\n-- neither needs pairing. The third is bound by POSITION against Prepare Model Prompt,\n-- which is the one pairing on this canvas that is not a name or an id, and it holds\n-- for the reason ar-ingest gives: the node above answers one item per item it ran on,\n-- so the item at this position was made from the item at the same position there.\n\n-- WHAT BEING WRONG ABOUT THE PAIRING WOULD COST IS LARGER HERE than it is at ingest,\n-- and it is worth saying so rather than inheriting the sibling's reassurance. There\n-- two calls swapping would swap two documents' sizes and leave the run total exact.\n-- Here the projection below carries pool_id, entity_id and the offered set, and the\n-- record group writes an entity_research row against them, so a swap would file one\n-- candidate's research under another candidate's subject and judge its citations\n-- against the wrong offered set. The run total is still exact and it is no longer the\n-- only thing being read.\n\n-- BOUND AS ONE JSONB VALUE rather than as a member per column: fewer values for the\n-- comma splitter to get wrong, and the members are read out here where a comment can\n-- say what each one is.\nprepared AS (\n  SELECT $3::jsonb AS item\n),\n\n-- What the row says about the call, cast once. prompt_chars and est_tokens are in the\n-- signal class the schema calls out: null means nothing measured this call, and zero\n-- is a real reading rather than a stand in for one. Neither is COALESCEd to zero.\n\n-- The node name is a literal and is known by construction: the step that made the\n-- call is the node above this one, and its own name is the one fact it cannot fail to\n-- have. It is Research Candidate rather than the endpoint hanging off it, because the\n-- endpoint is where the credential sits and the chain node is what a pass asked.\n\n-- The model is nullable and arrives so. A gateway that routes, or a runtime serving\n-- whatever it was started with, takes no model name, and what is stored is what was\n-- asked for rather than what served it.\ncall AS (\n  SELECT\n    nullif($2::text, '') AS model,\n    (p.item ->> 'prompt_chars')::integer AS prompt_chars,\n    (p.item ->> 'est_tokens')::integer AS est_tokens\n  FROM prepared p\n),\n\nwritten AS (\n  INSERT INTO llm_calls (run_id, node, model, prompt_chars, est_tokens)\n  SELECT\n    $1::bigint,\n    'Research Candidate',\n    c.model,\n    c.prompt_chars,\n    c.est_tokens\n  FROM call c\n  RETURNING id\n)\n\nSELECT\n-- The item this node answers, and it is about the candidate rather than about the\n-- call. The three ids come back out of the prepared item so the group below has the\n-- pool row to stamp and the subject to file a result under, and the offered set comes\n-- back with them because that is what researchBriefErrors judges an answer's\n-- citations against. Every one of them was derived above this node, and passing them\n-- through here is a carry rather than a second derivation.\n\n-- The answer the model gave is deliberately not among them: it is untrusted text, it\n-- is reachable off Research Candidate by name, and routing it through a SQL parameter\n-- to get here would be a round trip that changes nothing about it.\n\n-- offered_ids travels as jsonb rather than as a text list, so it arrives below in the\n-- shape it left the framing node in and nothing has to parse it back apart.\n\n-- llm_call_id is the ledger row this call left behind. Nothing below reads it today.\n-- It travels because the row and the item it was written for are otherwise joined by\n-- nothing at all, this table carrying no reference to a candidate.\n  $1::bigint AS run_id,\n  (SELECT (p.item ->> 'pool_id')::bigint FROM prepared p) AS pool_id,\n  (SELECT (p.item ->> 'domain_id')::bigint FROM prepared p) AS domain_id,\n  (SELECT (p.item ->> 'entity_id')::bigint FROM prepared p) AS entity_id,\n  (SELECT p.item -> 'offered_ids' FROM prepared p) AS offered_ids,\n  (SELECT w.id FROM written w) AS llm_call_id",
        "options": {
          "queryReplacement": "={{ $('Select Model Connector').first().json.run_id }}, {{ $('Select Model Connector').first().json.model }}, {{ JSON.stringify($('Prepare Model Prompt').all()[$itemIndex].json) }}"
        }
      }
    },
    {
      "id": "one-caller-no-clock",
      "name": "One Caller No Clock",
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        -460,
        -160
      ],
      "parameters": {
        "width": 400,
        "height": 1080,
        "content": "## One Caller, No Clock\n\nThis workflow is invoked, and by `ar-ingest` rather than by the dispatcher. A pull pass reaches it through an Execute Workflow node once it has written and scored what it found, and the Execute Workflow Trigger on this canvas is the whole of what makes that possible.\n\n`ar-dispatch` does not invoke it, and could not be made to without a table that does not exist. The only two schedulable tables are `topics` and `export_subscriptions`, and a research unit is neither of them. A pass here also needs a claimed topic in hand: the gap it may propose is clamped against that topic's own `min_interval_seconds` and `max_interval_seconds`, and the topic is what the dispatcher already claimed and handed on. So the invocation sits where the topic still is.\n\nIt holds no clock and may not acquire one: `ar-dispatch` carries the only Schedule Trigger in the system, and `tests/invariants/workflows.test.ts` counts them across every built workflow and names the one file allowed to hold one. The Execute Workflow Trigger here registers nothing when a workflow is activated, so `scripts/activate-workflows.sh` reports this file manual-only and leaves it inactive. That is the outcome rather than a gap: what starts this workflow is a caller, and a caller needs no registration.\n\n### What arrives\n\nOne item. `run_id` is the run the calling pass is accounting for, which is the row `ar-dispatch` opened for the claimed topic rather than one this workflow or its caller opened. The claim travels beside it: the topic's id and its two interval bounds, which are what a proposed gap is later clamped against.\n\n`Load Domain Context` reads the first of those and no more, and reads the domain back off the run rather than off a member of the item. The rest stays where it arrived, reachable at `$('Execute Workflow Trigger').first().json`, because this node's answer REPLACES the item the trigger was handed. Which node reads the claim is the group that proposes a gap, and reading it is that group's rather than this node's.\n\nThe trigger is typeVersion 1, which passes the caller's item through as it stands. Nothing here declares an input schema, so what the caller sends is what the first node reads, and adding a schema later is a change to both ends rather than to this node alone.\n\n### Started by hand\n\nA run started from this canvas rather than by a caller carries no `run_id`, and nothing pretends otherwise. `Load Domain Context` answers a null `run_id`, a null `domain_id`, a null domain row and four empty lists, which is a shape a node below can refuse by name. It is not an error worth suppressing: a pass with no run to charge has nowhere to write what it did, and a pass with no domain has no approved intention to drain, so it drains nothing rather than researching a candidate nobody asked about."
      }
    },
    {
      "id": "a-ruling-becomes-work",
      "name": "A Ruling Becomes Work",
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        420,
        -760
      ],
      "parameters": {
        "width": 400,
        "height": 1080,
        "content": "## A Ruling Becomes Work\n\n`Drain Approved Intentions` is where a person's decision stops being a row in a queue and starts costing something. Everything above it is about a domain. Everything below it is about the candidates somebody said yes to.\n\n`research_pool` in `src/db/schema/entities.ts` is the gate, and its whole argument is that noticing a subject is worth looking into and actually looking are separate events, written by different things at different times. `ar-ingest` and `ar-score` do the noticing, one intention per finding. A person does the ruling, through `scripts/approve.ts` today and through the API and the UI after that. This canvas does the looking, and only for what was ruled on.\n\n### A pending row is invisible to this query\n\nThe drain selects on a `status` of `approved` and a `researched_at` that is still NULL. A row nobody has looked at holds the `pending` default, fails the first of those, and is not selected, not counted, and not named by the group that closes the pass.\n\nThat is the outcome rather than a gap. A queue nobody has worked through drains nothing and spends nothing, so a run over a domain whose intentions are all pending researched no one and cost nothing. Read such a run as a queue waiting on a person, not as a pipeline that found nothing to do.\n\n### Nothing on this canvas approves anything\n\nNo node here writes `approved_at`, and no branch here stands in for one. A workflow able to approve its own intentions would be a second gate beside the one an operator uses, and it would sit where nobody reviews it: the instance is a deploy target rather than a source, so a guard changed on a canvas is lost at the next import and reaches no diff on the way.\n\nWhat refuses a row nobody approved is the database. `research_pool_approval_check` reads `approved_at` and `researched_at` and refuses any row recording that it was closed without recording that it was approved first. The record group below stamps `researched_at` in the SAME statement that inserts the `entity_research` row, so an unapproved subject takes neither half.\n\nThat check is the gate rather than a backstop behind one, and the drain does not duplicate it. `status` and the two timestamps are separate accounts of one row: nothing refuses a writer that sets `status` to `approved` without writing `approved_at`, so a row in that state IS drained here, does reach the model call, and is refused at the write. The call is billed and nothing is recorded. It is a misconfiguration somebody has to fix, and a run reporting a refused write is how they find out about it.\n\n### What the drain decides, and what it does not\n\nIt does not decide who is researched: that is the ruling, and it was made before this pass started. It does not decide what is asked: the strings are `research_pool.search_terms`, stored when the intention was raised so an operator reads the exact terms before they are sent rather than an account of what would be assembled later. It does not decide whether a candidate may be asked about at all: every drained name goes through `validateEntityName` in the gate below first.\n\nWhat it decides is how many and in what order. Oldest first, so a queue worked top-down empties rather than churns and the row that has waited longest is the one taken. Bounded, so a queue nobody is watching cannot become one expensive run, a hundred approved rows being researched over many passes rather than all at once. A row past the bound is not dropped: it keeps its approval and its open stamp, and it is at the head of the next pass."
      }
    }
  ],
  "connections": {
    "Execute Workflow Trigger": {
      "main": [
        [
          {
            "node": "Load Domain Context",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Load Domain Context": {
      "main": [
        [
          {
            "node": "Drain Approved Intentions",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Drain Approved Intentions": {
      "main": [
        [
          {
            "node": "Gate Candidate Names",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Gate Candidate Names": {
      "main": [
        [
          {
            "node": "Select Model Connector",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Select Model Connector": {
      "main": [
        [
          {
            "node": "Apply Call Ceiling",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Apply Call Ceiling": {
      "main": [
        [
          {
            "node": "Prepare Model Prompt",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Prepare Model Prompt": {
      "main": [
        [
          {
            "node": "Research Candidate",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Research Candidate": {
      "main": [
        [
          {
            "node": "Ledger Model Call",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Model Endpoint": {
      "ai_languageModel": [
        [
          {
            "node": "Research Candidate",
            "type": "ai_languageModel",
            "index": 0
          }
        ]
      ]
    }
  }
}
