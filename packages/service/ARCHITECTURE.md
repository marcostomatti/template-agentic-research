# Architecture — the index of `docs/architecture/`

The platform shape and the same-commit doc-update law live in
[`docs/architecture/00-overview.md`](docs/architecture/00-overview.md);
this file is the index of that directory and holds nothing else.

| Document | What it covers |
| --- | --- |
| [`docs/architecture/00-overview.md`](docs/architecture/00-overview.md) | The three parts and the one schema they share, the layout of `packages/service`, the core vocabulary, which document covers each behaviour area and the law that keeps it true, and the test harness. |
| [`docs/architecture/01-invariants.md`](docs/architecture/01-invariants.md) | The register of platform-wide invariants: what each one is, the artifact that fails when it stops holding, the phase that lands that artifact, and whether it is enforced today. |
| [`docs/architecture/02-schema.md`](docs/architecture/02-schema.md) | Schema v2: the tables by area, the rules the database holds itself rather than leaving to whoever writes the row — the null-vs-zero classes, the `documents.hash` dedupe pair, the schedulable-row contract, the approval CHECK and the category depth cap — the roster of declared indexes and what reads each, and which of the constraints the generated migration owns and which the hand-written one. |
| [`docs/architecture/03-workflows.md`](docs/architecture/03-workflows.md) | The workflow set: the six workflows and the phase that lands each, the one-file-per-workflow rule, what a workflow envelope and its nodes may hold and the four homes a decision made at a node has, and the build over `workflows/src/` — the two marker forms a source may write and the two it refuses, the three rules a spliced library obeys and the two of them the build can see, why two runs over one tree write the same bytes, and why the transpile needs bun rather than a test worker. |
| [`docs/architecture/04-sources.md`](docs/architecture/04-sources.md) | The source adapter contract: the five members an adapter declares and the three things it may not do, the kind set that selects one and the single declaration both its readers are derived from, the canonical document as a slice of a `documents` row rather than a translation of one, the I/O boundary — why `fetch` is the only async member, what a stored payload stands in for, and what fail-flag-keep needs from it — the source row bound at construction rather than per call and the per-source branch that keeps out of the adapter, and the text reduction those adapters share: the structure it preserves, what it refuses to invent, and the characters it drops. |
| [`docs/architecture/06-scheduling.md`](docs/architecture/06-scheduling.md) | The one scheduling mechanism: the two tables that take part and the five timing columns a schedulable row carries, the four ways `next_run_at` gets written and the three answers `runs.scheduled_by` leaves for them, the claim `ar-dispatch` runs — its predicate, its ordering, its lock, the cap it applies twice, why the reschedule is folded into it, and what committing between the two would cost — and the run row opened per claimed unit rather than per tick, with the limits that leaves: no row for a tick claiming nothing, no column naming the workflow that opened one, and every run failed until the targets land. |
| [`docs/architecture/07-auth.md`](docs/architecture/07-auth.md) | The one authentication strategy: the two tables and what each column is for — why a session carries its own subject, and why `revoked_at` is a timestamp rather than a boolean — the presence toggle that decides whether any of it is registered, what a blank value means there and which pair is the fallback, the bootstrap as a managed dependency ordered behind Postgres, why migrations stay an operator step, and the upsert that proposes a subject and imposes only the hash; then the two introspection paths and the one flat 401 the login route answers, and the rules the hashes live under: the two files that may name one, the argon2id parameters written down rather than defaulted, and why a session token is SHA-256 instead. |
| [`docs/architecture/08-http-api.md`](docs/architecture/08-http-api.md) | The HTTP route surface and the rules its resource groups share, wave 1 landed, wave 2 written before its routes and its topics group landed against that writing: the `{ success: true, data, meta? }` success envelope beside the framework's own `{ code, message, details? }` failure envelope — why that asymmetry is cheaper than reshaping vendored code, and the four failures that answer in neither shape; the `?page`/`?perPage` vocabulary with its default, its cap refused rather than clamped, its derived `meta`, and the page past the end that is an empty list rather than a 404; `ctx.requireAuth` on every route with the reads not argued down, sitting on the mount rather than the handler, and what a credential-free local boot therefore cannot prove; the validation rule that a detail names a field path and never a submitted value — the one zod issue kind measured to leak, the container an `unrecognized_keys` detail names in place of the key, and the `*` a path below an open record collapses to; the root-absolute paths each of the five wave-1 routers declares, what forces them, and the `/auth` mount recorded as the exception; and wave 2 mostly ahead of its code — the four prefixes it adds beside the two it borrows, the pending-config pair deferred to q13 with the reason that survived, the three routes permitted to write `next_run_at` and the disabled-row and unscheduled-row refusals that bound them, the columns the pipeline owns and this surface answers but never accepts, the write-only connector-secret mask with the read-modify-write round trip its refusal closes, and the read-only failures queue with its total order, its control-byte masking and its code-point body cap; then the first of those four groups against the rules above — a topic met in its domain and written by its id, created unscheduled until a verb schedules it, its name unique within the domain from both writes that can propose one, its term list replaced whole while its two interval bounds distinguish three requests, and a delete nothing in schema v2 can refuse. |
